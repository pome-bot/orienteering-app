orienteerings/new

= form_with model: @orienteering, local: true do |f|
  - if @orienteering.errors.any?
    .setting-group-form__errors
      %h2= "#{@orienteering.errors.full_messages.count}件のエラーが発生しました。"
      %ul
        - @orienteering.errors.full_messages.each do |message|
          %li= message
  .setting-group-form__field
    = f.label :name, "Orienteering name", class: 'setting-group-form__label'
    = f.text_field :name, class: 'setting-group-form__input', placeholder: 'input orienteering name'
  .setting-group-form__field
    = f.submit class: 'setting-group-form__button'
      
      